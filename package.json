{
  "name": "thomas-baker-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.159.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "terser": "^5.26.0",
    "vite": "^5.0.0"
  }
}
