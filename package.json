{
  "name": "space-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "setup-textures": "mkdir -p public/textures",
    "download-textures": "node scripts/download-textures.js",
    "prepare-textures": "npm run setup-textures && npm run download-textures"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "gh-pages": "^6.1.1"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.170.0"
  }
}
